export interface Book {
    _id: string,
    title: string,
    author: string,
    publisher: string,
    condition: string,
    image: string,
    date: Date,
    email: string,
    type: string,
    createdAt: Date,
    updatedAt: Date
}